# Fen

Git client fully written in Rust

## About the project

The project is fully written in Rust with the `no_std` attribute, without dependencies and C externs, for Windows.

Absolutely everything was written manually, without using ready-made solutions.

